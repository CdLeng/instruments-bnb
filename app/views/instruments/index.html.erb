<ul>
  <% @instruments.each do |instrument| %>
    <li><%= link_to "#{instrument.name}", instrument_path(instrument) %></li>
  <% end %>
</ul>

<%# <p><%= link_to "Book this instrument", tasks_new_path(@task)%></p> %>
